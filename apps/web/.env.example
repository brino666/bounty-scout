# Copy to .env.local for local dev (usually not needed — the dev server
# already proxies /api to the api app on :8080). Set this as a Vercel
# Environment Variable on the WEB project once both are deployed, pointing
# at your deployed API project's URL, e.g. https://hackerone-scout-api.vercel.app
VITE_API_URL=
